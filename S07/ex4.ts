let numbers = [100, 34, 5, 67, 72];
// En utilisant la récursion, écrivez le code permettant de trouver le min et le max de cette liste

function min( tab:Array<number>): number{
    let min = 
    
    return 0;
}


let minimum = 
let maximum = 
console.log(`Min is ${minimum}, max is ${maximum}`);